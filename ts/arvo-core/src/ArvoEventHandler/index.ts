import type { AsyncResult } from "../types.js";

export class ArvoEventHandler {
  //constructe uses the params in tuypes

  async tryExecute(param: {event, state}) : AsyncResult<{events: [], state}>

  async execute(...) is the twijn of tryExeucte

}

